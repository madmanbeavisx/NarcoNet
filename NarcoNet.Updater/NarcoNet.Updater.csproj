<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net472</TargetFramework>
        <LangVersion>latest</LangVersion>
        <Nullable>enable</Nullable>
        <UseWindowsForms>true</UseWindowsForms>
        <IsPackable>false</IsPackable>
        <Version>1.0.0</Version>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Newtonsoft.Json" Version="13.0.4"/>
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\NarcoNet.Utilities\NarcoNet.Utilities.csproj"/>
    </ItemGroup>

    <Target Name="PostBuild" AfterTargets="PostBuildEvent" Condition="'$(SolutionDir)' != '' AND '$(SolutionDir)' != '*Undefined*'">
        <PropertyGroup>
            <BuildOutputDir>$(SolutionDir)Build</BuildOutputDir>
        </PropertyGroup>

        <ItemGroup>
            <UpdaterFiles Include="$(TargetDir)NarcoNet.Updater.exe"/>
            <UpdaterFiles Include="$(TargetDir)Newtonsoft.Json.dll"/>
        </ItemGroup>

        <ItemGroup>
            <UtilitiesFiles Include="$(TargetDir)NarcoNet.Utilities.dll"/>
        </ItemGroup>

        <!-- Create output directory structure -->
        <MakeDir Directories="$(BuildOutputDir)"/>
        <MakeDir Directories="$(BuildOutputDir)\BepInEx\plugins\MadManBeavis-NarcoNet"/>

        <!-- Copy updater and dependencies to base Build directory -->
        <Copy SourceFiles="@(UpdaterFiles)" DestinationFolder="$(BuildOutputDir)"/>

        <!-- Copy utilities to both locations -->
        <Copy SourceFiles="@(UtilitiesFiles)" DestinationFolder="$(BuildOutputDir)"/>
        <Copy SourceFiles="@(UtilitiesFiles)" DestinationFolder="$(BuildOutputDir)\BepInEx\plugins\MadManBeavis-NarcoNet"/>

        <Message Text="Updater EXE and dependencies copied to: $(BuildOutputDir)" Importance="high"/>
    </Target>

</Project>
